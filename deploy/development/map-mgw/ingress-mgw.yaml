apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: makeaplea-mgw
  namespace: makeaplea-dev
  annotations:
    external-dns.alpha.kubernetes.io/set-identifier: makeaplea-mgw-makeaplea-dev-green
    external-dns.alpha.kubernetes.io/aws-weight: "100"
    nginx.ingress.kubernetes.io/auth-tls-secret: "makeaplea-dev/mtls-certs"
    nginx.ingress.kubernetes.io/auth-tls-verify-client: "on"
    nginx.ingress.kubernetes.io/auth-tls-verify-depth: "2"

spec:
  ingressClassName: default
  tls:
    - hosts:
      - dev-map-mgw.apps.live.cloud-platform.service.justice.gov.uk
      - dev-map-mgw-admin.apps.live.cloud-platform.service.justice.gov.uk
      secretName: mtls-certs

  rules:
    - host: dev-map-mgw.apps.live.cloud-platform.service.justice.gov.uk
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: map-mgw-service
                port:
                  number: 8080
    - host: dev-map-mgw-admin.apps.live.cloud-platform.service.justice.gov.uk
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: map-mgw-service
                port:
                  number: 9990